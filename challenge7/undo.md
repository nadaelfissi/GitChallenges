# Undo in Git

This file explains the difference between git restore and git reset.

## git restore
- Used to cancel changes in working directory
- Does not affect commit history
- Safe operation

## git reset
- Used to move HEAD to a previous commit
- Can modify commit history
- More dangerous if used incorrectly
This line will be removed.